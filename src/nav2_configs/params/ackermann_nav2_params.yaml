behavior_server:
  ros__parameters:
    backup:
      plugin: nav2_behaviors/BackUp
      simulate_ahead_time: 2.0
      speed: 0.1
      time_allowance: 8.0
    behavior_plugins:
    - backup
    - drive_on_heading
    - wait
    - spin
    drive_on_heading:
      plugin: nav2_behaviors/DriveOnHeading
      simulate_ahead_time: 2.0
      speed: 0.2
      time_allowance: 6.0
    global_frame: odom
    robot_base_frame: base_link
    spin:
      max_rotational_vel: 0.5
      min_rotational_vel: 0.1
      plugin: nav2_behaviors/Spin
      rotational_acc_lim: 0.8
      simulate_ahead_time: 1.0
      time_allowance: 6.0
    transform_tolerance: 0.3
    use_sim_time: false
    wait:
      plugin: nav2_behaviors/Wait
      time_allowance: 10.0
bt_navigator:
  ros__parameters:
    bt_loop_duration: 50                 # 增加到50ms (20Hz)，降低计算负载
    default_controller_id: FollowPath
    default_nav_through_poses_bt_xml: /opt/ros/humble/share/nav2_bt_navigator/behavior_trees/navigate_through_poses_w_replanning_and_recovery.xml
    default_nav_to_pose_bt_xml: /home/jetson/ros2_ws/src/nav2_configs/behavior_trees/ackermann_navigate_to_pose.xml
    default_planner_id: AckermannHybridAStar
    default_server_timeout: 20
    global_frame: map
    odom_topic: /odom
    plugin_lib_names:
    - nav2_compute_path_to_pose_action_bt_node
    - nav2_compute_path_through_poses_action_bt_node
    - nav2_smooth_path_action_bt_node
    - nav2_follow_path_action_bt_node
    - nav2_spin_action_bt_node
    - nav2_wait_action_bt_node
    - nav2_back_up_action_bt_node
    - nav2_drive_on_heading_bt_node
    - nav2_clear_costmap_service_bt_node
    - nav2_is_stuck_condition_bt_node
    - nav2_goal_reached_condition_bt_node
    - nav2_goal_updated_condition_bt_node
    - nav2_globally_updated_goal_condition_bt_node
    - nav2_is_path_valid_condition_bt_node
    - nav2_initial_pose_received_condition_bt_node
    - nav2_reinitialize_global_localization_service_bt_node
    - nav2_rate_controller_bt_node
    - nav2_distance_controller_bt_node
    - nav2_speed_controller_bt_node
    - nav2_truncate_path_action_bt_node
    - nav2_truncate_path_local_action_bt_node
    - nav2_goal_updater_node_bt_node
    - nav2_recovery_node_bt_node
    - nav2_pipeline_sequence_bt_node
    - nav2_round_robin_node_bt_node
    - nav2_transform_available_condition_bt_node
    - nav2_time_expired_condition_bt_node
    - nav2_path_expiring_timer_condition
    - nav2_distance_traveled_condition_bt_node
    - nav2_single_trigger_bt_node
    - nav2_is_battery_low_condition_bt_node
    - nav2_navigate_through_poses_action_bt_node
    - nav2_navigate_to_pose_action_bt_node
    - nav2_remove_passed_goals_action_bt_node
    - nav2_planner_selector_bt_node
    - nav2_controller_selector_bt_node
    - nav2_goal_checker_selector_bt_node
    robot_base_frame: base_link
    use_sim_time: false
controller_server:
  ros__parameters:
    FollowPath:
      AckermannConstraints:
        min_turning_r: 0.3047
      ConstraintCritic:
        cost_power: 1
        cost_weight: 8.0
        enabled: true
        weight: 4.0
      CostCritic:
        collision_cost: 1000000.0         # 保持高碰撞成本
        consider_footprint: true
        cost_power: 1
        cost_weight: 4.0                  # 🔧 降低成本权重，减少过度避障
        critical_cost: 300.0              # 🔧 提高临界成本，允许更接近障碍物
        enabled: true
        near_goal_distance: 0.8
        weight: 100.0                     # 🔧 降低整体权重，减少避障过敏
      ObstaclesCritic:                    # 障碍物避障评价器
        collision_margin_distance: 0.08   # 🔧 减少碰撞边界距离，允许更接近障碍物
        cost_power: 1
        cost_weight: 6.0                  # 🔧 降低权重，减少过度避障
        enabled: true
        repulsion_weight: 1.5             # 🔧 降低排斥权重
        weight: 50.0                      # 🔧 降低整体权重
      GoalAngleCritic:
        cost_power: 1
        cost_weight: 4.0
        enabled: true
        threshold_to_consider: 0.4
        weight: 2.0
      GoalCritic:
        cost_power: 1
        cost_weight: 6.0
        enabled: true
        threshold_to_consider: 1.2
        weight: 3.0
      PathAlignCritic:
        cost_power: 1
        cost_weight: 10.0
        enabled: true
        max_path_occupancy_ratio: 0.07
        offset_from_furthest: 15
        threshold_to_consider: 0.4
        trajectory_point_step: 2
        use_path_orientations: false
        weight: 5.0
      PathAngleCritic:
        cost_power: 1
        cost_weight: 3.0
        enabled: true
        forward_preference: true
        max_angle_to_furthest: 0.8
        offset_from_furthest: 3
        threshold_to_consider: 0.4
        weight: 1.5
      PathFollowCritic:
        cost_power: 1
        cost_weight: 7.0
        enabled: true
        offset_from_furthest: 4
        threshold_to_consider: 1.0
        weight: 8.0
      PreferForwardCritic:
        cost_power: 1
        cost_weight: 3.0
        enabled: true
        threshold_to_consider: 0.3
        weight: 2.0
      batch_size: 200                     # 降低批次大小，减少计算负载
      critics:
      - ConstraintCritic
      - ObstaclesCritic                   # 添加障碍物避障评价器
      - CostCritic
      - GoalCritic
      - GoalAngleCritic
      - PathAlignCritic
      - PathFollowCritic
      - PathAngleCritic
      - PreferForwardCritic
      gamma: 0.015
      iteration_count: 1
      model_dt: 0.1                       # 匹配10Hz控制器频率 (1/10 = 0.1)
      motion_model: Ackermann
      plugin: nav2_mppi_controller::MPPIController
      prune_distance: 1.5
      reset_period: 0.5
      temperature: 0.2
      time_steps: 50                      # 恢复时间步数，保持5秒预测窗口
      transform_tolerance: 0.15
      visualize: false
      vx_max: 0.4                        # 测绘模式：进一步降低最大速度，提高建图精度
      vx_min: -0.2                       # 测绘模式：降低最大倒车速度，避免高速倒车
      vx_std: 0.1
      vy_max: 0.0
      vy_std: 0.05
      wz_max: 1.0
      wz_std: 0.2
    ackermann_goal_checker:
      plugin: nav2_controller::SimpleGoalChecker
      stateful: true
      xy_goal_tolerance: 0.4
      yaw_goal_tolerance: 0.5
    controller_frequency: 10.0           # 恢复到10Hz，匹配model_dt
    controller_plugins:
    - FollowPath
    failure_tolerance: 0.3
    goal_checker_plugins:
    - ackermann_goal_checker
    min_theta_velocity_threshold: 0.001
    min_x_velocity_threshold: 0.001
    min_y_velocity_threshold: 0.001
    progress_checker:
      movement_time_allowance: 30.0
      plugin: nav2_controller::SimpleProgressChecker
      required_movement_radius: 0.5
    progress_checker_plugins:
    - progress_checker
    use_sim_time: false
global_costmap:
  global_costmap:
    ros__parameters:
      allow_unknown: true
      always_send_full_costmap: false
      footprint: '[[0.08, 0.05], [0.08, -0.05], [-0.08, -0.05], [-0.08, 0.05]]'
      footprint_padding: 0.01             # 🔧 减少安全边界到1cm，允许通过更窄的空间
      global_frame: map
      height: 40
      inflation_layer:
        cost_scaling_factor: 1.8          # 🔧 降低成本缩放因子，减少过度避障
        enabled: true
        inflate_around_unknown: false
        inflate_unknown: false
        inflation_radius: 0.35             # 🔧 大幅减少膨胀半径，允许通过走廊
        plugin: nav2_costmap_2d::InflationLayer
      lethal_cost_threshold: 100
      obstacle_layer:
        enabled: true
        observation_sources: scan
        plugin: nav2_costmap_2d::ObstacleLayer
        scan:
          clearing: true
          data_type: LaserScan
          marking: true
          max_obstacle_height: 2.0
          obstacle_max_range: 6.0          # 🔧 减少障碍物检测范围，避免远距离误判
          obstacle_min_range: 0.15       # � 减少最小距离，提高近距离检测精度
          raytrace_max_range: 8.0        # 🔧 减少光线追踪范围
          raytrace_min_range: 0.15       # � 减少光线追踪最小距离
          topic: /scan
      origin_x: -30.0
      origin_y: -30.0
      plugins:
      - obstacle_layer
      - inflation_layer
      publish_frequency: 1.0
      resolution: 0.03
      robot_base_frame: base_link
      track_unknown_space: true
      transform_tolerance: 1.0           # 🚨 增加变换容忍度，提高稳定性
      trinary_costmap: true
      unknown_cost_value: -1
      update_frequency: 2.0
      use_maximum: false
      use_sim_time: false
      width: 40
lifecycle_manager:
  ros__parameters:
    autostart: true
    node_names:
    - controller_server
    - planner_server
    - smoother_server
    - behavior_server
    - bt_navigator
    use_sim_time: false
local_costmap:
  local_costmap:
    ros__parameters:
      always_send_full_costmap: true
      footprint: '[[0.08, 0.05], [0.08, -0.05], [-0.08, -0.05], [-0.08, 0.05]]'
      footprint_padding: 0.01             # 🔧 减少安全边界到1cm，允许通过更窄的空间
      footprint_clearing_enabled: true
      global_frame: odom
      height: 4
      inflation_layer:
        cost_scaling_factor: 1.8          # 🔧 降低成本缩放因子，减少过度避障
        enabled: true
        inflate_around_unknown: false
        inflate_unknown: false
        inflation_radius: 0.35             # 🔧 大幅减少膨胀半径，允许通过走廊
        plugin: nav2_costmap_2d::InflationLayer
      plugins:
      - voxel_layer
      - inflation_layer
      publish_frequency: 2.0
      resolution: 0.03
      robot_base_frame: base_link
      rolling_window: true
      transform_tolerance: 0.3
      update_frequency: 5.0
      use_sim_time: false
      voxel_layer:
        clear_threshold: 1
        combination_method: 1
        enabled: true
        mark_threshold: 2
        max_obstacle_height: 1.5
        min_obstacle_height: 0.0
        observation_sources: scan
        obstacle_range: 2.0
        origin_z: 0.0
        plugin: nav2_costmap_2d::VoxelLayer
        publish_voxel_map: false
        raytrace_range: 2.5
        scan:
          clearing: true
          data_type: LaserScan
          marking: true
          max_obstacle_height: 1.5
          min_obstacle_height: 0.0
          obstacle_max_range: 6.0          # 🔧 减少障碍物检测范围，避免远距离误判
          obstacle_min_range: 0.15       # 🔧 减少最小距离，提高近距离检测精度
          obstacle_range: 1.5             # 🔧 减少障碍物范围
          raytrace_max_range: 8.0        # 🔧 减少光线追踪范围
          raytrace_range: 2.5
          topic: /scan
        z_resolution: 0.05
        z_voxels: 16
      width: 4
map_server:
  ros__parameters:
    frame_id: map
    topic_name: map
    use_sim_time: false
    yaml_filename: ''
planner_server:
  ros__parameters:
    AckermannHybridAStar:
      allow_primitive_interpolation: true
      allow_reverse: false
      allow_unknown: true
      analytic_expansion_max_length: 3.0
      angle_quantization_bins: 64
      cache_obstacle_heuristic: true
      change_penalty: 0.05
      cost_penalty: 1.5                 # 🔧 降低成本惩罚，减少过度避障
      cost_penalty_penalty: 1.5         # 🔧 降低成本惩罚的惩罚
      cost_travel_multiplier: 1.2       # 🔧 降低行驶成本倍数，允许更接近障碍物
      debug_visualizations: false
      downsample_costmap: false
      lookup_table_size: 20.0
      max_iterations: 2000000
      max_on_approach_iterations: 3000
      max_planning_time: 15.0
      minimum_turning_radius: 0.25
      motion_model_for_search: REEDS_SHEPP
      non_straight_penalty: 1.1
      plugin: nav2_smac_planner/SmacPlannerHybrid
      retrospective_penalty: 0.01
      reverse_penalty: 1.5
      smoothing: true
      tolerance: 1.0
      allow_unknown: true                # 允许在未知区域规划路径
      use_final_approach_orientation: false
      use_quadratic_cost_penalty: true
    expected_planner_frequency: 1.0
    planner_plugins:
    - AckermannHybridAStar
    use_sim_time: false
smoother_server:
  ros__parameters:
    simple_smoother:
      do_refinement: true
      max_its: 1000
      plugin: nav2_smoother::SimpleSmoother
      refinement_num: 2
      tolerance: 1.0e-10
    smoother_plugins:
    - simple_smoother
    use_sim_time: false
waypoint_follower:
  ros__parameters:
    loop_rate: 20
    stop_on_failure: true
    use_sim_time: false
    wait_at_waypoint:
      enabled: true
      plugin: nav2_waypoint_follower::WaitAtWaypoint
      waypoint_pause_duration: 0
    waypoint_task_executor_plugin: wait_at_waypoint
